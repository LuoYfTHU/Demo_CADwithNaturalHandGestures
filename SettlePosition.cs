using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;
using System.IO;


public class SettlePosition : MonoBehaviour
{
    GameObject controller;
    GameObject PC_Set;

    Vector3 pc_position;
    Vector3 controller_position;

    // Start is called before the first frame update
    void Start()
    {
    //     transform controller = GameObject.Find("OVRPlayerController");
    //    //GameObject PC_Set = GameObject.Find("PC_Set");

    //   //  pc_position = PC_Set.transform.position;
    //     controller_position = new Vector3(0, 1, 1);
    //     controller.transform.position = controller_position;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
